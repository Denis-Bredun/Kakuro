<UserControl x:Class="Kakuro.Views.StopwatchSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kakuro.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock VerticalAlignment="Center" Grid.Column="1" HorizontalAlignment="Center" FontSize="45" Foreground="Red"
           Visibility="{Binding IsTimerVisible, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock.Inlines>
                <Run Text="{Binding StopWatchHours}" />
                <Run Text=":" />
                <Run Text="{Binding StopWatchMinutes}" />
                <Run Text=":" />
                <Run Text="{Binding StopWatchSeconds}" />
            </TextBlock.Inlines>
        </TextBlock>


    </Grid>
</UserControl>
