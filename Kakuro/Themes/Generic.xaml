<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Kakuro.Styles.Custom_Controls">


    <Style TargetType="{x:Type local:SumCell}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SumCell}">
                    <Grid Background="Black">
                        <TextBlock Text="{TemplateBinding SumRight}" 
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Top"
                                   Foreground="White"
                                   FontSize="14"
                                   Margin="0,5,0,0" />

                        <Path Data="M 10 10 L 40 40" Stroke="White" StrokeThickness="1" />

                        <TextBlock Text="{TemplateBinding SumBottom}" 
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Bottom"
                                   Foreground="White"
                                   FontSize="14"
                                   Margin="5,0,0,0" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
